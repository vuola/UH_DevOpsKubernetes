services:
  frontend:
    image: frontend-test
    build:
      context: frontend-test
      args:
        - _PORT_=${_PORT_}
        - _NGINX_PORT_=${_NGINX_PORT_}
    volumes:
      - /tmp/www/html:/var/www/html
    ports:
      - ${_PORT_}:${_NGINX_PORT_}
